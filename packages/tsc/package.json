{
  "name": "${__PACKAGE_NAME__}",
  "version": "0.0.1",
  "author": "${__PACKAGE_AUTHOR__}",
  "description": "",
  "main": "./lib/index.js",
  "sideEffects": false,
  "scripts": {
    "start": "tsc -p tsconfig.prod.json --watch",
    "build": "tsc -p tsconfig.prod.json"
  },
  "homepage": "${__PACKAGE_REPO_HOME__}",
  "repository": {
    "type": "git",
    "url": "${__PACKAGE_REPO_GIT__}"
  },
  "bugs": {
    "url": "${__PACKAGE_REPO_ISSURE__}"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0"
  }
}
